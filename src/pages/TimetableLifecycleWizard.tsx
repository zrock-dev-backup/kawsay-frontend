import React from "react";
import { Typography } from "@mui/material";

const TimetableLifecycleWizard: React.FC = () => {
  return <Typography variant="h5">Timetable Wizard</Typography>;
};

export default TimetableLifecycleWizard;
